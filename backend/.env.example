# JWT Secret compartido entre SIE y LOPDP-EC
# Debe ser el MISMO valor en ambos sistemas
# Generar con: openssl rand -base64 64
# ⚠️  NO COMMITEAR EL VALOR REAL — usar este archivo solo como plantilla
JWT_SECRET=cambiar-por-valor-seguro-en-produccion-min-256-bits
